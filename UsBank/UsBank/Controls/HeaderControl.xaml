<UserControl
    x:Class="UsBank.Controls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UsBank.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UsBank.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="RootHeaderControl">

    <Grid Height="120">
        <Grid.Background>
            <SolidColorBrush Color="#e2cdb1"/>
        </Grid.Background>
        <Grid HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding ElementName=RootHeaderControl,Path=UserName}" FontSize="20" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding ElementName=RootHeaderControl,Path=UserRole}" FontSize="20" HorizontalAlignment="Right"/>
            </StackPanel>
            <Image Grid.Column="1" Source="/Assets/Pic1.png"/>
        </Grid>
    </Grid>
</UserControl>
